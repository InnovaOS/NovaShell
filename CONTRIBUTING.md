# Contributing
- Create a virtualenv: `python3 -m venv .venv && source .venv/bin/activate`
- Install: `python -m pip install -e .[dev]`
- Run tests: `pytest -q`
- Style: `ruff check .`
- PRs welcome. Keep commands stable and add tests.
